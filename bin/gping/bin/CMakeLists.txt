# See LICENSE file in the project root for license information.

set(EXECUTABLE_NAME "${PROJECT_NAME}-gping")
file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}")
file(CREATE_LINK "${PROJECT_NAME}-runpy" "${CMAKE_CURRENT_BINARY_DIR}/${EXECUTABLE_NAME}" SYMBOLIC)
install(FILES "${CMAKE_CURRENT_BINARY_DIR}/${EXECUTABLE_NAME}" DESTINATION "${CMAKE_INSTALL_BINDIR}" PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE WORLD_EXECUTE)
