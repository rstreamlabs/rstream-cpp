name: Build Dev
on:
  push:
    branches:
      - feature-**
      - fix-**
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        build:
          - {
              conan:
                {
                  channel: "dev",
                  profiles: { build: "default", host: "default" },
                },
            }
    uses: ./.github/workflows/conan.yml
    secrets: inherit
    with:
      build: ${{ toJson(matrix.build) }}
